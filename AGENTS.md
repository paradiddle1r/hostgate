# HostGate agent hand-off

Before starting work, read `~/AI_PROJECT_STATUS.md`, then read `CLAUDE.md` in
this repository. `CLAUDE.md` is the single source of truth for architecture,
feature scope, database conventions, verification, and deployment notes.

After a work session, update the HostGate block in `~/AI_PROJECT_STATUS.md`
and bump its `Last updated` stamp.

